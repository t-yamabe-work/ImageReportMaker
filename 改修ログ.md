# 画像報告書メーカー — 改修ログ（リポジトリ）

> プロジェクト全体の意思決定ログは Obsidian 保管庫内
> `画像報告書アプリを作ろう/改修ログ.md` を参照。
> こちらはリポジトリ内の実装作業ログ。

## 2026-04-21 プロジェクト骨組み生成

### 目的
- Illustrator + JSXスクリプト運用を置き換える macOSネイティブアプリを作る
- 初期リポジトリ構造を確立し、Core（ロジック）／App（UI）の分離を担保する

### 作成したもの
- `.gitignore` — KAKUSUベース、ビルド成果物・Xcodeユーザー設定・機密画像除外
- `README.md` — 概要・セットアップ・構成
- `Core/Package.swift` — SPMパッケージ定義（`ReportCore` モジュール）
- `Core/Sources/ReportCore/` — Models / Layout / Rendering / Persistence の骨格
- `project.yml` — xcodegen設定（`ImageReportMaker` アプリ、macOS 13+ターゲット）
- `Apps/ImageReportMaker/` — SwiftUIアプリのエントリ＋最小View
- `scripts/build-and-install.sh` — `/Applications/` 配置スクリプト
- `docs/layout-spec.md` — JSXから抽出した数値仕様表
- `docs/jsx-reference.md` — 元JSXの参照コピーと移植メモ

### 次のアクション
- [ ] `xcodegen generate` でXcodeプロジェクト生成確認
- [ ] ビルド通過確認（空のアプリが起動するところまで）
- [ ] 社長AIに引き渡し、worker1/2/3で並列実装
  - worker1: `Core/Sources/ReportCore/` 実装（GridLayoutCalculator、ReportRenderer、SVGExporter）
  - worker2: `project.yml` 調整、Info.plist、アイコン、ビルド設定
  - worker3: `Apps/ImageReportMaker/Views/` 実装（フォーム、D&D、プレビュー）

## 2026-04-21 worker2: 仮AppIcon整備

### 目的
- `/Applications/画像報告書メーカー.app` にアプリアイコンが反映される状態を作る
- Assets カタログ経由で Xcode ビルドに AppIcon を取り込む

### 実施内容
- `scripts/generate-icons.swift` を新規作成
  - NSGraphicsContext + CoreGraphics で青グラデ角丸背景＋白文字「画報」を描画
  - 16/32/128/256/512 の @1x / @2x（合計10枚）を一括PNG出力
  - Hiragino Sans W6 を使用（フォント未取得時は boldSystemFont フォールバック）
- `Apps/ImageReportMaker/Resources/Assets.xcassets/AppIcon.appiconset/`
  - `Contents.json`（macOS 用10エントリ）
  - 生成済みPNG 10枚を配置
- `Apps/ImageReportMaker/Resources/Assets.xcassets/Contents.json` を追加
- `project.yml`
  - targets.ImageReportMaker.info.properties に `CFBundleIconName: AppIcon` を追記
  - targets.ImageReportMaker.settings.base に `ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon` を追加
- `Apps/ImageReportMaker/Info.plist`
  - `CFBundleIconName = AppIcon` を追記

### 確認結果
- `xcodebuild ... Debug build` 成功
- `/Applications/画像報告書メーカー.app/Contents/Resources/` に `AppIcon.icns` と `Assets.car` 生成を確認
- `CFBundleIconName` = AppIcon、`CFBundleDisplayName` = 画像報告書メーカー が Info.plist に出力されていることを確認
- `scripts/build-and-install.sh` 実行で `/Applications/画像報告書メーカー.app` 再配置成功

### 次のアクション
- 本格アイコンは v1.0.0 で差し替え（仮アイコン運用）

## 2026-04-21 v0.1.0 MVP 実装完了（worker1/2/3 並列）

### worker1: ReportCore 実装
- `Layout/GridLayoutCalculator.swift` — JSX `simulateGridLayout` を Swift 移植。`minColumns`〜`maxColumns` 範囲で全高 ≤ `maxBlockHeightMm` となる最小列数を選択、満たせない場合は `maxColumns`。
- `Rendering/ReportRenderer.swift` — `CGContext` + CoreText + `ImageIO` で A4 オフスクリーン描画（ヘッダー／本文／画像グリッド／外枠）。JPG/PNG エンコード、SVG は `SVGExporter` に委譲。
- `Rendering/SVGExporter.swift` — Hiragino Sans W3/W6 TTC を Base64 で `@font-face` 埋め込み、画像は `data:image/png;base64,...` インライン。
- `Core/Tests/ReportCoreTests/` — 列数決定エッジケース、mm↔pt 往復、JPG/PNG/SVG シグネチャ（計 15 tests）。
- コミット: 46cfa6a [worker1]

### worker3: UI 実装
- `Views/HeaderFormView.swift` — 氏名 TextField + 日付 DatePicker + 曜日自動表示。
- `Views/CaseListView.swift` — ●タイトル+→詳細リスト、追加/削除/.onMove。
- `Views/ImageDropZoneView.swift` — NSItemProvider による png/jpg D&D、サムネ、削除。
- `Views/PreviewView.swift` — `ReportRenderer.render` 結果を `NSImage` で `.scaleToFit`。
- `Views/ExportPanel.swift` — JPG/PNG/SVG セレクト、ファイル名デフォルト `YYMMDD.jpg`、前回値記憶、初回デスクトップ。
- `ContentView.swift` — 左フォーム群＋右プレビューの 2ペイン構成。
- `ViewModels/ReportViewModel.swift` — addCase / removeCase / moveCase / addImages / removeImage / export / refreshPreview。
- コミット: 92b15f2 / 9c4b367 / cb9ed05 / 54d791e / fe628e3 [worker3]

### 統合確認（boss）
- `xcodegen generate` 成功
- `xcodebuild -scheme ImageReportMaker -configuration Debug build` 成功（BUILD SUCCEEDED、警告ゼロ）
- `swift test`（Core）: 15 tests 全緑
- strict concurrency（`SWIFT_STRICT_CONCURRENCY: complete`）準拠
- ReportCore public API は初版のまま（boss 経由の変更なし）

### 完了基準の状況
- [x] `xcodebuild Debug build` 成功
- [x] `xcodebuild test` で `ReportCoreTests` 全緑（15/15）
- [ ] 起動→氏名/日付/案件入力→画像D&D→プレビュー→JPG 書き出し（`~/Desktop/YYMMDD.jpg`）の一連動作 — 人間による GUI 実動作確認待ち
- [ ] JSX 出力との視覚的近似 — 同上

### 次のアクション
- 社長AI → 人間（運用者）に実動作確認を依頼
- 差分があれば v0.1.5 で調整

## 2026-04-21 v0.1.1 レイアウトフィードバック反映（worker1）

### 目的
- 人間運用者からのフィードバック5点を Core レイアウトに反映

### 実施内容（Core/Sources/ReportCore/ 配下のみ）
1. **`formatHeaderDate` 短縮化**（`ReportRenderer.swift`）
   - 旧: `yyyy年M月d日(EEEEE) 画像報告`
   - 新: `M/d（EEEEE）　画像報告`（例: `4/21（火）　画像報告`、全角括弧＋全角スペース）
2. **ヘッダー日付の見切れ対策**（`ReportRenderer.swift`）
   - `fittedFontSize(text:weight:baseSize:maxWidth:)` を追加
   - CTLine で実幅を計測し、右パディング込みでページ幅を超える場合のみ等比縮小
   - `PreparedLayout.dateFontSizePt` に保存し、CGContext/SVG 両方が同じ値を使用
3. **点線をグレー背景の上下端に**（`ReportRenderer.swift` / `SVGExporter.swift`）
   - ハードコード値 `138.95` を廃止
   - `dashedTopYPt = grayTopYPt`、`dashedBottomYPt = grayTopYPt + grayHeightPt` として動的算出
   - CGContext 描画 / SVG 出力 いずれも 2 本の点線を出力
4. **案件テキスト上マージン削減**（`ReportRenderer.swift`）
   - `bodyStartYPt = dashedRuleYPt + 20` → `solidRuleYPt + 15`
5. **画像ブロックへの距離短縮**（`LayoutConstants.swift`）
   - `textToBlockGapMm: 6.0 → 3.0`（SVGExporter は `prepareLayout` 結果経由で自動反映・二重描画なし）

### テスト更新
- `testHeaderDateFormatIsJapanese` を `testHeaderDateFormatShort` にリネーム
- 期待値: `画像報告` を含む／`年` `月` を含まない／曜日1文字の漢字（月火水木金土日）である／2026-04-21 に対し `火` / `4/21` / `（` / `）` を含む
- swift test: 15/15 全緑維持

### ビルド／インストール確認
- `xcodebuild -configuration Debug build`: BUILD SUCCEEDED
- `scripts/build-and-install.sh`: `/Applications/画像報告書メーカー.app` 更新完了

## 2026-04-21 v0.1.2 詳細折り返し＋150dpi（worker1）

### 目的
- フィードバック2点を Core に反映
  - W1-A: `→詳細` の右端見切れを解消（折り返し＋ハンギングインデント）
  - W1-B: 書き出し時の解像度を 150dpi に引き上げ（プレビューは 72dpi 維持）

### 公開 API 変更（worker3 は以下を参照して呼び出し側を修正）
- `RenderOptions` の **デフォルト dpi を 72 → 150** に変更
  - 既存呼び出し `RenderOptions(format: .png, dpi: 72.0)` は破壊なし（明示指定はそのまま通る）
  - 引数省略時は 150dpi となる
- `RenderOptions` に **factory** を追加：
  - `RenderOptions.export(format:jpegQuality:)` — 書き出し用プリセット（dpi=150, quality=0.92）
  - `RenderOptions.preview(format:)` — プレビュー用プリセット（dpi=72, quality=0.85、format デフォルト `.jpg`）
- 推奨移行：
  - プレビュー呼び出し → `RenderOptions.preview(format: .png)` または `.preview()`
  - 書き出し呼び出し → `RenderOptions.export(format: selectedFormat)`
  - これによりViewModel側の `previewDpi`/`exportDpi` 定数は撤去可能

### Core 実装
- `LayoutConstants.swift`
  - `caseDetailLineHeightMultiple: 1.25` を追加
  - `caseDetailOffsetPt: 38.13` を集約（既存ハードコード撤去）
- `Rendering/ReportRenderer.swift`
  - `wrapText(_:fontSize:weight:contentWidthPt:firstLineIndentPt:continuedIndentPt:)` を追加
    - `CTTypesetterSuggestLineBreak` で行ブレーク位置を計算
    - 1行目は `firstLineIndentPt = 0`、2行目以降は `continuedIndentPt = arrowWidthPt` のハンギングインデント
  - `measureLineWidth(text:fontSize:weight:)` を追加（行幅実測）
  - `PreparedLayout` を再設計
    - 固定の `caseLineHeightPt` / `caseDetailOffsetPt` を削除
    - `cases: [CaseBlock]` を保持。各案件に `titleBaselineYPt`、`detailFirstBaselineYPt`、`detailLineHeightPt`、`detailLines: [WrappedLine]`、`totalHeightPt` を持たせる
    - 複数案件は折り返し後の実高さで縦スタック
  - 描画: 折り返し済みの各行を `baseline + i × lineHeight` の位置に描画
- `Rendering/SVGExporter.swift`
  - 同じ `CaseBlock` を参照し、1行につき `<text>` 要素を1つ出力（x=leftPt+indent、y=baseline）

### テスト（21/21 緑）
- `testRenderOptionsDefaultDpiIs150`
- `testRenderOptionsExportPreset`
- `testRenderOptionsPreviewPreset`
- `testWrapTextLongDetailProducesMultipleLines` — 長文が >1 行に折り返され、続く行にインデントが付く
- `testCaseStackingHeightGrowsWithLongDetail` — 長文案件ありのほうがページ全高・case.totalHeight が大きく、detailLines.count が複数
- `testPreviewAndExportDpiProduceDifferentByteSizes` — 72dpi vs 150dpi で PNG サイズが違う

### ビルド／インストール確認
- `cd Core && swift test`: 21/21 緑
- `cd Core && swift build`: 成功
- `xcodebuild Debug build`: `Apps/ImageReportMaker/ViewModels/ReportViewModel.swift` で worker3 側の `previewDpi`/`exportDpi` 定数が MainActor 分離エラー — 新 factory（`.preview()` / `.export(format:)`）への差し替えで解消される想定（worker3 担当）

## 2026-04-21 v0.1.3 worker3: 詳細文デフォルト／案件記憶／衝突ポリシー

### 目的
- 人間から FB 3 点。毎日のルーチン入力を軽くする＋書き出し事故を防ぐ

### W3-I: 詳細文の初期値を「を進行いたしました。」に
- `AppPreferences.defaultCaseDetail = "を進行いたしました。"` を定数化
- `ReportViewModel.init` の初期ケース、`addCase()`、`removeCase(at:)` の空復帰いずれも同文言で始まる状態に統一
- 空文字スタートから変更。ユーザーは案件名と主語句を書くだけで済む

### W3-J: 一番上の案件を永続化
- `AppPreferences` に `topCaseTitle: String?` / `topCaseDetail: String?` 追加
- `ReportViewModel.init` で復元（無ければ W3-I のデフォルト）
- `bindTopCasePersistence()` で `$cases` を Combine 監視し、`cases[0]` 変更のたびに両キーを保存（`dropFirst()` で init 直後の上書きを回避）
- 2 件目以降は記憶対象外（要件どおり）

### W3-K: 同名ファイル衝突の挙動選択
- `FileCollisionPolicy` enum 追加: `overwrite` / `warn`（デフォルト） / `sequential`
- `AppPreferences.collisionPolicy` に永続化
- `ExportPanel` に「同名ファイル」セグメントピッカー（上書き / 警告 / 連番）
- `ReportViewModel.export()` の URL 決定前に `resolveExportURL(base:)` を通す
  - overwrite: そのまま `.atomic` で上書き
  - sequential: `nextAvailableURL(base:)` で `stem_2.ext`, `stem_3.ext`... の空き番を探す（上限 10,000）
  - warn: NSAlert（上書き／連番で保存／キャンセル）。キャンセル時は `lastErrorMessage = "書き出しキャンセル"` を立てて抜ける

### 変更ファイル（Apps 側のみ）
- `Apps/ImageReportMaker/ViewModels/AppPreferences.swift`
  - `defaultCaseDetail` 定数、`topCaseTitle` / `topCaseDetail` / `collisionPolicy` アクセサ、`FileCollisionPolicy` enum
- `Apps/ImageReportMaker/ViewModels/ReportViewModel.swift`
  - `Combine` インポート、`cancellables`、`bindTopCasePersistence()`
  - init の cases 復元、`addCase()` / `removeCase(at:)` のデフォルト文言差し替え
  - `@Published var collisionPolicy`、`persistCollisionPolicy()`
  - `resolveExportURL(base:)` / `nextAvailableURL(base:)` 追加
- `Apps/ImageReportMaker/Views/ExportPanel.swift`
  - `collisionRow`（セグメント）追加、`onChange` で永続化

### ビルド確認
- `xcodebuild -project ImageReportMaker.xcodeproj -scheme ImageReportMaker -configuration Debug build`: 成功（警告ゼロ維持）
- Core 側は非変更のため `swift test` は 21/21 緑のまま想定

---

## 次回対応予定（v0.1.4 候補） — ユーザー配布後フィードバック

配布後、社内ユーザーから寄せられた要望。worker3 担当想定。

### F-1. 案件の明示的な削除ボタン
- **現状**: CaseListView は `.onMove` / `.onDelete` を使っており、macOSでは List のスワイプ削除か右クリック削除に依存
- **問題**: ユーザー（機械音痴想定）がそれに気づかず「間違えて追加した案件を消す方法が分からない」
- **対応案**: 各案件カードの右上に明示的な × ボタン（削除）を表示。`ViewModel.removeCase(at:)` を直接呼ぶ。確認ダイアログは出さず即時削除、ただし cases が1件のみのときは削除不可にして確認不要

### F-2. 詳細文だけ追加するボタン
- **現状**: 1 案件 = ●タイトル + →詳細 × 1 の固定構造
- **問題**: 同じ案件内で複数の詳細行を書きたいとき、新規案件を追加してしまう（タイトル部分を空にすれば見た目は近い、が不自然）
- **要件確認が必要**:
  - (A) 1 案件に複数の `→詳細` 行を持たせる（`ReportCase` を `title: String, details: [String]` に変更）
  - (B) ●タイトルなしの「詳細のみエントリ」を追加できるようにする（`ReportCase` の title を空文字で許容し、レンダリング側で ● をスキップ）
  - どちらの仕様にするか、次回のキックオフ時に人間に確認
- **レンダリング側への影響**（worker1 担当範囲も発生）:
  - (A) の場合: `ReportCore.ReportCase` のモデル変更、`ReportRenderer.prepareLayout` の `CaseBlock` 構築ループで詳細行を複数レンダー、SVGExporter 同様
  - (B) の場合: `ReportCase` 構造は維持、描画側で title が空ならタイトル行を描画しないだけ（軽微）

### F-3. 画像の並び替えUI
- **現状**: D&D で追加した順に並ぶ。入れ替えは不可
- **対応案**: ImageDropZoneView のサムネ一覧を並び替え可能に。以下のいずれか:
  - (A) 各サムネの下に ← / → ボタン（小さめ）
  - (B) サムネ自体をドラッグで位置入れ替え（LazyVGrid + onDrag + onDrop）
  - (C) List ベース化して `.onMove` を使う（最も単純）
- worker3 担当

### F-4. 太字（Bold）の撤去
- **現状**: レンダリング出力で以下が W6（太字）:
  - 氏名（ヘッダー中央）
  - 各案件の ●タイトル行
- **要件**: すべて W3（レギュラー）に統一
- **対応箇所**:
  - `ReportRenderer.swift` の drawHeader / drawBody で `weight: .w6` → `.w3`
  - `SVGExporter.swift` の textElement 呼び出しで `cls: "t-w6"` → `"t-w3"`
- worker1 担当

### F-5. 案件タイトル・詳細文のフォントサイズ縮小
- **現状**: `LayoutConstants.caseTitleFontSizePt = 22.30` / `caseDetailFontSizePt = 21.29`
- **要件**: もう少し小さく。具体値は未確定
- **案**: タイトル 18.0pt / 詳細 17.0pt あたりから試して、実機レビューで調整
- **注意**: `caseDetailLineHeightMultiple = 1.25` は維持でOK。折り返しロジックもそのまま
- worker1 担当（LayoutConstants のみの変更）

### 着手時の段取り
1. F-2 の仕様（A / B / A+B）を人間に確認
2. F-5 の具体値候補（タイトル/詳細それぞれ）を人間に確認（推奨値で進めて調整もOK）
3. F-3 の UI 方式（A/B/C）を人間に確認
4. worker1（F-4・F-5 と F-2 モデル変更部分）と worker3（F-1・F-3 と F-2 UI部分）にそれぞれタスクを切り出し
5. v0.1.4 としてまとめてリリース

